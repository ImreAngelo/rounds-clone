## Install Instructions


### Setup on Linux
1. Setup steam
`sudo snap install steam`

2. Symlink steam to `~/.steam/`
`ln -s ~/snap/steam/common/.steam ~/.steam`

<!-- #### Setup on WSL
3. Download the [Steamworks SDK](https://partner.steamgames.com/doc/sdk)
4. Place the sdk in `~/.steamworks` or update the makefile to point to the correct path -->